<UserControl x:Class="Pedidos_WpfApp.Views.ProdutosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Cadastro de Produtos" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <GroupBox Grid.Row="1" Header="Filtros" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="Nome"/>
                    <TextBox Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="Código"/>
                    <TextBox Text="{Binding FiltroCodigo, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="5">
                    <TextBlock Text="Valor Inicial"/>
                    <TextBox Text="{Binding FiltroValorInicial, 
                      UpdateSourceTrigger=PropertyChanged, 
                      StringFormat=N2,
                      TargetNullValue=''}" 
                    Margin="0,2,0,0"/>
                </StackPanel>
                <StackPanel Grid.Column="3" Margin="5">
                    <TextBlock Text="Valor Final"/>
                    <TextBox Text="{Binding FiltroValorFinal, 
                      UpdateSourceTrigger=PropertyChanged, 
                      StringFormat=N2,
                      TargetNullValue=''}" 
                    Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="4" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="5">
                    <Button Content="Pesquisar" Command="{Binding PesquisarCommand}" Margin="2" Padding="10,5"/>
                    <Button Content="Limpar" Command="{Binding LimparFiltrosCommand}" Margin="2" Padding="10,5"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Produtos}" 
                  SelectedItem="{Binding ProdutoSelecionado}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" Width="100"/>
                <DataGridTextColumn Header="Valor" Binding="{Binding Valor, StringFormat=C}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Grid.Row="3" Header="Dados do Produto">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="Nome *"/>
                    <TextBox Text="{Binding ProdutoSelecionado.Nome, UpdateSourceTrigger=PropertyChanged}" 
                             IsEnabled="{Binding CamposEditaveis}"
                             Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="Código *"/>
                    <TextBox Text="{Binding ProdutoSelecionado.Codigo, UpdateSourceTrigger=PropertyChanged}" 
                             IsEnabled="{Binding CamposEditaveis}"
                             Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="5">
                    <TextBlock Text="Valor *"/>
                    <TextBox Text="{Binding ProdutoSelecionado.Valor, 
                      UpdateSourceTrigger=PropertyChanged, 
                      StringFormat=N2,
                      TargetNullValue=''}"
                    IsEnabled="{Binding CamposEditaveis}"
                    Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="5">
                    <Button Content="Novo" Command="{Binding NovoProdutoCommand}" Margin="2" Padding="10,5"/>
                    <Button Content="Incluir" Command="{Binding IncluirCommand}" Margin="2" Padding="10,5"/>
                    <Button Content="Editar" Command="{Binding EditarCommand}" Margin="2" Padding="10,5"/>
                    <Button Content="Salvar" Command="{Binding SalvarCommand}" Margin="2" Padding="10,5"/>
                    <Button Content="Excluir" Command="{Binding ExcluirCommand}" Margin="2" Padding="10,5"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>