<UserControl  x:Class="Pedidos_WpfApp.Views.PessoasView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"

        mc:Ignorable="d" 
        d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Cadastro de Pessoas" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <GroupBox Grid.Row="1" Header="Filtros" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="Nome"/>
                    <TextBox Text="{Binding FiltroNome, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="CPF"/>
                    <TextBox Text="{Binding FiltroCPF, UpdateSourceTrigger=PropertyChanged}" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="5">
                    <Button Content="Pesquisar" Command="{Binding PesquisarCommand}" Margin="2" Padding="10,5"/>
                    <Button Content="Limpar" Command="{Binding LimparFiltrosCommand}" Margin="2" Padding="10,5"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding Pessoas}" 
                  SelectedItem="{Binding PessoaSelecionada}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="CPF" Binding="{Binding CPF}" Width="120"/>
                <DataGridTextColumn Header="Endereço" Binding="{Binding Endereco}" Width="200"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Grid.Row="3" Header="Dados da Pessoa">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="Nome *"/>
                    <TextBox Text="{Binding PessoaSelecionada.Nome, UpdateSourceTrigger=PropertyChanged}" 
                             IsEnabled="{Binding CamposEditaveis}"
                             Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="CPF *"/>
                    <TextBox Text="{Binding PessoaSelecionada.CPF, UpdateSourceTrigger=PropertyChanged}" 
                             IsEnabled="{Binding CamposEditaveis}"
                             Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="5">
                    <TextBlock Text="Endereço"/>
                    <TextBox Text="{Binding PessoaSelecionada.Endereco, UpdateSourceTrigger=PropertyChanged}" 
                             IsEnabled="{Binding CamposEditaveis}"
                             Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Bottom" Margin="5">
                    <Button Content="Novo" Command="{Binding NovaPessoaCommand}" Margin="2" Padding="10,5"/>
                    <Button Content="Incluir" Command="{Binding IncluirCommand}" Margin="2" Padding="10,5"/>
                    <Button Content="Editar" Command="{Binding EditarCommand}" Margin="2" Padding="10,5"/>
                    <Button Content="Salvar" Command="{Binding SalvarCommand}" Margin="2" Padding="10,5"/>
                    <Button Content="Excluir" Command="{Binding ExcluirCommand}" Margin="2" Padding="10,5"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
